<?php

	/*		
	  $db = new mysqli('localhost', 'CrooksC', "lulls.dug", 'CrooksC');
  	 if ($db->connect_error):
   	   die ("Could not connect to db " . $db->connect_error);
  	 endif;

		$user = $_SESSION['username']; 
		
	  $query = "select num_wins from users where username = '$user'";
      
      
   
      $result = $db->query($query);
      $row = $result->fetch_array();
      $count = $row["num_wins"];
     // $newcount = $count + 1;
    //  $query = "update users set num_wins = '$newcount' where users = '$user'";
     // $result = $db->query($query) || die("Error");
 
 
 
*/ 
 
 
 
 
      echo "OK";
  
?>
